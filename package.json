{
  "name": "xinjia-zhao-website",
  "version": "1.0.0",
  "private": true,
  "description": "Personal academic website of Xinjia Zhao (Hugo + PaperMod)",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "tools/hugo.exe --gc --minify",
    "preview": "tools/hugo.exe server --port 7100 --bind 0.0.0.0 --disableFastRender"
  }
}
